export interface AppConfig {
    port: string,
    host: string,
    limitBodySize: string,
    redisPort: string,
    redisHost: string,
    redisDatabase: string,
    redisConnectionTimeout: string
}